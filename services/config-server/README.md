# TrendBurada Config Server

Central configuration service for TrendBurada applications.

## Run

```bash
./mvnw -q spring-boot:run
```

## Package

```bash
./mvnw -q package
```

## Endpoints

- `GET /trend-burada-platform-app/dev`
- `GET /trend-burada-platform-app/docker`
